$(document).ready(function() {
  
    // var selectOption = function() {
    // var $stationSelect = $( ".start-station option:selected" ).text();
    //   console.log($stationSelect);
    // };

    // ALERT INPUTS
    var startLine = $('#start-station').val();
    var startStation = $('#start-station').val();
    var endLine = $('#end-station').val();
    var endStation = $('#end-station').val();

    $('#planTrip'.on('click', alert("Your trip: " + trip + " with " + tripLength + " stops."));

});