<h1>Weather Chart</h1>

<canvas id="weather" width="800" height="400"></canvas>


<!-- NOT USING AJAX 
<script>
  var maximums = <%#= Temperature.pluck(:maximum).to_json %>;
  var minimums = <%#= Temperature.pluck(:minimum).to_json %>;

  $(document).ready(function() {
    var $chart = $('#weather');
    if ($chart.length === 0) {
      return;
    }
    var ctx = $chart.get(0).getContext('2d');
    var myLineChart = new Chart(ctx).Line(data);

</script> -->